/** Copy to config.js. Local dev: Pexels uses this key in the browser. */
window.ISTUDIO_PEXELS_KEY = "";

/** For “Send by email” on inspire page. */
window.ISTUDIO_CONTACT_EMAIL = "";

/** Netlify production: also set PEXELS_API_KEY in Site → Environment variables (see netlify/functions/pexels-search.js). */
